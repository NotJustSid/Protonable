{
  "name": "protonable",
  "version": "1.0.0",
  "description": "A Minimal Text Editor Built on Electron!",
  "main": "index.js",
  "scripts": {
    "start": "electron .",
    "package-mac": "electron-packager . --overwrite --platform=darwin --arch=x64 --icon=img/icon.icns --prune=true --out=release-builds",
    "package-win": "electron-packager . protonable --overwrite --asar=true --platform=win32 --arch=ia32 --icon=img/icon.ico --prune=true --out=release-builds --version-string.CompanyName=Sid --version-string.FileDescription=A Minimal Text Editor Built on Electron! --version-string.ProductName=\"Protonable\"",
    "package-linux": "electron-packager . protonable --overwrite --asar=true --platform=linux --arch=x64 --icon=img/icon.png --prune=true --out=release-builds"
  },
  "author": "Siddhanth Bhowmick",
  "license": "MIT",
  "dependencies": {
    "@fortawesome/fontawesome-free": "^5.5.0",
    "codemirror-colorpicker": "^1.9.48",
    "electron": "^3.0.7",
    "electron-packager": "^13.0.1",
    "mousetrap": "^1.6.2",
    "os": "^0.1.1",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "electron-builder": "^20.36.2"
  }
}
